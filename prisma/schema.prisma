generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native", "linux-musl"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id        String @id
  login     String
  password  String
  version   Int
  createdAt Float
  updatedAt Float
}

model Track {
  id       String  @id
  name     String
  artistId String?
  albumId  String?
  duration Int
  fav Boolean?
}

model Artist {
  id     String  @id
  name   String
  grammy Boolean
  fav Boolean?
}

model Album {
  id       String  @id
  name     String
  year     Float
  artistId String?
  fav Boolean?
}
